<!-- res/drawable/rounded_key_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/black" /> <!-- Background color of the key -->
    <corners android:radius="1000dp" /> <!-- Adjust the radius for rounded corners -->
    <stroke
        android:width="1dp"
        android:color="@color/magenta" /> <!-- Optional: Add a border -->
</shape>